#include "constants.h"
#include "classes.h"
#include <raylib.h>

// settings
int fpsc = 144;
Vector2 windowSize = (Vector2){600, 800};

// color-related
const Color backgroundColor = (Color){255, 0, 255, 255};
const Color errorSBackgroundColor = (Color){200, 0, 0, 255};